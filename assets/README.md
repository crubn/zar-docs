# Assets

Shared images for the docs and `README.md`.

## Committed

| File | Used by |
|--|--|
| `banner.svg` | `README.md` hero |
| `favicon.svg` | `docs.json` favicon |

Both are vector (SVG), so they stay crisp at any size and are easy to edit by hand. Swap in a raster `banner.png` / `favicon.png` later if you prefer — just update the references in `README.md` and `docs.json`.

## Suggested screenshots (not yet added)

Add these to replace prose with real product shots where the docs reference them:

| File | Used by | Type |
|--|--|--|
| `pr-comment.png` | Quickstart / PR feedback | screenshot |
| `dashboard.png` | Dashboard page | screenshot |
| `ci-gate-check.png` | CI gate page | screenshot |
